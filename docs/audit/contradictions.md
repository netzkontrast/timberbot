# Contradiction log

Rule (kickoff §3.3): when README, docs, comments, the kickoff prompt and code disagree, **code wins**, but the disagreement is logged here. Anchors are `path:line` in this repo at commit `d21988a`. Confidence: HIGH = read in code, MEDIUM = inferred from code + docs, LOW = not verifiable offline.

| # | Claim (source) | Code says | Anchor(s) | Confidence | Consequence |
|---|---|---|---|---|---|
| C1 | Kickoff §0: repo is "a clone of `abix-/TimberbornMods` v0.7.1, Apr 2026" | This repo is a byte-identical mirror of `impuls42/timberbot` `main` (same HEAD sha `d21988a`), which is itself a modified fork of abix-. Mod version is **0.8.0**, Python package **0.9.0**. abix- upstream `master` still ships manifest `0.7.1` (fetched from raw.githubusercontent, 2026-09-02). | `README.md:7`, `timberbot/src/manifest.json:3`, `timberbot/src/Timberbot.csproj:6`, `python/pyproject.toml:7`, `mkdocs.yml:3-5`; `git ls-remote` of both remotes | HIGH | Two upstreams to track (abix- for the mod core, impuls42 for everything else). Git history here is re-rooted (50 commits, root `df5cc98` has no parent), so `git merge upstream` will not work by ancestry. |
| C2 | Kickoff §0: "No Timberborn MCP server exists anywhere yet; this is the greenfield part" | A FastMCP server with 61 game tools + 9 delegation tools exists and is started by `tbot serve` on `127.0.0.1:8091` over SSE. | `python/src/timberbot/game_mcp/server.py:102-112`, `python/src/timberbot/user_api/serve.py:746-751`, `docs/configuration.md:96-97` | HIGH | Phase 2 ADR-001 is "migrate/replace" not "greenfield". The existing server is 2025-era (SSE, stateful, no annotations, no MRTR). |
| C3 | Kickoff §1: settings `webhookBatchMs` 200, `webhookCircuitBreaker` 30 | Both keys are on the deprecated list and ignored with a log line. Outbound webhooks were removed in the WebSocket cutover; there is no HTTP delivery path. | `timberbot/src/TimberbotPure.cs:34-41`, `timberbot/src/TimberbotService.cs:208-215`, `timberbot/src/TimberbotEvents.cs:7-11`, `docs/unreleased.md` ("v0.9 WebSocket cutover") | HIGH | Settings inventory in the audit lists only the live keys. |
| C4 | Kickoff §1 + abix- README: "68 webhook events" | 68 distinct event names exist, but they are pushed as WebSocket `event` frames, not webhooks. 64 handlers in `TimberbotEvents`, 4 in `TimberbotEntityRegistry`. | `timberbot/src/TimberbotEvents.cs:87-194`, `timberbot/src/TimberbotEntityRegistry.cs:157-171`, `docs/events.md:92` | HIGH | Count is right, transport is wrong. |
| C5 | README.md:150 credits TOON "for compact AI output"; kickoff §1 "TOON-encoded output option" | The mod's `format=toon` produces **flat JSON** (arrays of flat objects, occupants as `"Name:z"` strings). Real TOON encoding happens only client-side in the CLI via `toons.dumps`. The wire format is always JSON. | `timberbot/src/TimberbotHttpServer.cs:305-306`, `timberbot/src/TimberbotReadV2.cs:944-951`, `timberbot/src/TimberbotReadV2.cs:1235-1247`, `python/src/timberbot/cli/main.py:206-209`, `python/src/timberbot/api/client.py:3-8` | HIGH | ADR-002 must measure TOON client-side vs minified JSON; the mod has no TOON. `docs/api-reference.md:18` is accurate ("Requires `pip install toons`"), the README wording is not. |
| C6 | README.md:71 "Write job system. Budgeted frame execution, no spikes" | `LambdaWriteJob.Step` runs the whole action in a single call; the per-frame budget only applies to multi-step jobs (`RoutePathJob`, `FindPlacementJob`, benchmark). `place_building` executes unbounded inside one frame, including a `PreviewFactory.Create` + destroy. | `timberbot/src/ITimberbotWriteJob.cs:54-65`, `timberbot/src/TimberbotHttpServer.cs:561`, `timberbot/src/TimberbotPlacement.cs:2314-2440`, `docs/architecture.md:74` ("blocks game: yes, for duration") | HIGH | ADR-005 needs to state which writes are budgeted and which are atomic-per-frame. |
| C7 | README.md:72 "Zero-alloc hot path. No garbage collection pressure on read endpoints" | Steady-state reads are alloc-light, not zero-alloc: response `ToString()` per request (100-500 KB), `new char[maxBodyBytes+1]` (2 MB) per POST body, `new List<(string,int)>` per newly seen tile key, `new int[]` per species/district in summary. `docs/performance.md` itself lists these as "accepted allocations". | `timberbot/src/TimberbotHttpServer.cs:358`, `timberbot/src/TimberbotReadV2.cs:1150,1164,1178`, `timberbot/src/TimberbotReadV2.cs:599,606,625`, `docs/performance.md:86-92` | HIGH | Marketing claim; performance doc is the accurate source. |
| C8 | `TimberbotHttpServer.cs:51` comment: "separate JW for HTTP-layer errors (thread-safe: not shared with read/write paths)" | The same `_jw` instance is written from the listener thread (401/500/413/400 paths) **and** from the Unity main thread (drain/unknown-endpoint/write-job error paths and the agent handlers). One `StringBuilder`, two threads, no lock. | listener: `TimberbotHttpServer.cs:270,338,365,380`; main: `TimberbotHttpServer.cs:152,164,185,197,220,483,535,544,600,615,632,641,649` | HIGH | Latent race: concurrent error responses can interleave JSON. Low frequency, real. **Fixed 2026-09-02:** `TimberbotHttpServer` now uses a `[ThreadStatic]` writer (`Jw`). |
| C9 | `TimberbotHttpServer.cs:277` comment lists `/api/tbot/*` in the ready-gate carve-out | `IsGateExempt` exempts only `/api/ping`, `/api/ready`, `/api/agent`, `/api/agent/*`. `/api/tbot/*` routes were deleted in the WS cutover. | `timberbot/src/TimberbotAgentState.cs:357-365`, `timberbot/src/TimberbotHttpServer.cs:568-572` | HIGH | Stale comment only. |
| C10 | `TimberbotHttpServer.cs:5,29` + `docs/architecture.md:46-48`: POSTs "drained on Unity main thread (max 10/frame)" | Ten requests per frame are *admitted* into `_writeQueue`, but `ProcessWriteJobs` runs **one** active job at a time and the default `settleFrames=1` makes every `LambdaWriteJob` occupy at least two frames. Effective write throughput is ≤ 0.5 jobs/frame, serialized. | `timberbot/src/TimberbotHttpServer.cs:140-167,172-224`, `timberbot/src/ITimberbotWriteJob.cs:42,54-73` | HIGH | Matters for ADR-005 (tick semantics) and for agent latency budgets; not documented anywhere. |
| C11 | AGENTS.md:47 "All game DLLs are referenced with `Publicize="true"` to access internal APIs **without reflection**" | Reflection is used outside the debug inspector: prefab cost properties (`GoodId`/`Id`/`Amount`), nav-mesh internals (`_nodeIdService`, `_accessCoordinates`, `_navigationRangeService`, `GetRoadNodesInRange`), and settlement-name discovery (`SaveReference`, `SettlementReference`, `SettlementName`). | `timberbot/src/TimberbotPlacement.cs:266-267,1653-1678,1944-1969`, `timberbot/src/TimberbotReadV2.cs:1531-1540` | HIGH | These string-literal member names are the highest drift exposure in the mod; they are invisible to the compiler and must be in the Phase 1 drift detector. |
| C12 | `docs/architecture.md:60` diagram: "step pending write jobs (2ms budget)" vs `docs/architecture.md:76` and `ITimberbotWriteJob.cs:7`: "default 1ms" | Code default is `1.0` ms. | `timberbot/src/TimberbotService.cs:67,183-187` | HIGH | Doc-internal inconsistency. |
| C13 | `docs/api-reference.md:28-33,1358-1366` shows error bodies; kickoff §1 assumes a "legible error contract" | Domain errors from write jobs and unknown endpoints are returned with **HTTP 200** and an `{"error": "code: message", ...}` body; only transport-level failures use 400/401/409/413/500. Placement validation errors carry no code prefix at all (e.g. `"occupied by Path at (120,130,2). demolish it or …"`). The "hint" is embedded in the message string, not a separate field. | `timberbot/src/ITimberbotWriteJob.cs:38,75-81`, `timberbot/src/TimberbotHttpServer.cs:152,185,211,333,472`, `timberbot/src/TimberbotJw.cs:149-156`, `timberbot/src/TimberbotPlacement.cs:2323-2380` | HIGH | Direct input to ADR-003. The Python client compensates by raising on any `error` key (`python/src/timberbot/api/client.py:135-179`). |
| C14 | `docs/architecture.md:190-193` + `design/thread-safe-surfaces.md:110-127`: "listener-thread code must not walk live Timberborn entity/component graphs"; `BuildingService`, `BuildingUnlockingService` are "main-thread only" | `GET /api/prefabs` runs on the listener thread and iterates the live `_buildingService.Buildings` list, calling `_buildingUnlockingService.Unlocked(bs)` per building. `GET /api/tiles` calls `SoilContaminationService.SoilIsContaminated` / `SoilMoistureService.SoilIsMoist` off-thread (the design doc classifies these as "verify first"). | `timberbot/src/TimberbotHttpServer.cs:326-333,468-469`, `timberbot/src/TimberbotPlacement.cs` (`CollectPrefabs`, iterates `_buildingService.Buildings`), `timberbot/src/TimberbotReadV2.cs:1215-1216` | HIGH | Refutes H1 for one endpoint. Also `Placement.Jw` is shared between the main-thread `place_building` job and the listener-thread `prefabs` read (`TimberbotHttpServer.cs:561` vs `CollectPrefabs` `Jw.Reset()`), a second cross-thread `StringBuilder` race. **Partially addressed 2026-09-02:** the MCP tool `timberborn_get_prefabs` runs `CollectPrefabs` as a main-thread job (`mcp:prefabs` route); `GET /api/prefabs` and `/api/tiles` are unchanged. |
| C15 | README.md:139 "Requirements: .NET SDK 6+" | The mod targets `netstandard2.1` (any modern SDK builds it), but the xUnit project targets `net10.0` and CI installs .NET 10. | `timberbot/src/Timberbot.csproj:4`, `timberbot/test/Timberbot.Tests.csproj:4`, `.github/workflows/dotnet-tests.yml:35` | HIGH | Phase 1 environment doc must require .NET 10 SDK for `make test`. |
| C16 | `python/src/timberbot/formatters/colors.py:3-4`: "The CLI is responsible for ensuring the output stream supports [ANSI]" | No `isatty`/`NO_COLOR` check exists anywhere in the package; `render_map` always emits ANSI escapes. When an agent runs `tbot map` through a shell tool, the escapes land in the model context. | `python/src/timberbot/formatters/map.py:109-117,185-192`, grep `isatty/NO_COLOR` over `python/src` → 0 hits | HIGH | Measured cost: ≈7× the token count of the plain map (see audit §6). |
| C17 | `docs/timberbot.md:162` tells the agent "Use the brain map for fast orientation and `tiles` when you need raw data" | The MCP server exposes neither a `map` nor a `tiles` tool; an agent driven through `tbot serve` has no spatial read at all. `tiles` is reachable only via the REST client/CLI. | `python/src/timberbot/game_mcp/server.py:121-413` (tool list; no `map`/`tiles`), `python/src/timberbot/api/client.py:499,706` | HIGH | Observation gap G-OBS-1 in the audit. |
| C18 | Kickoff §1 + README.md:147-151 credits: `TemplateInstantiator`, `MarkAsPreviewAndInitialize`, `TreeCuttingArea.AddCoordinates()`, `PlantingService.SetPlantingCoordinates()`, `PlantingAreaValidator.CanPlant()`, `BuildingUnlockingService.Unlock()/Unlocked()`, `PreviewFactory`, `BlockValidator`, `BlockObjectPlacerService.Place()` | `TemplateInstantiator` and `MarkAsPreviewAndInitialize` do **not** appear in code (zero hits; `Timberborn.TemplateInstantiation` is a dead csproj reference). `Place()` is called on the placer returned by `GetMatchingPlacer`, arity 3 with a callback. `BlockValidator` is reached only through the publicized private field `_blockValidator`. The rest are present with the signatures listed in `docs/audit/appendix-b-symbol-inventory.md` §2. | `timberbot/src/TimberbotPlacement.cs:1716,2245-2249,2314-2340`, `timberbot/src/TimberbotWrite.cs:498,584,589,717`; grep over `timberbot/src/*.cs` | HIGH | README credits are provenance of *ideas*, not a symbol inventory; the drift detector must be seeded from Appendix B, not from the README. |
| C19 | `docs/architecture.md:132` and README.md:66: ready gate returns 409 for "all `/api/*` calls (read + write) except `/api/agent/*`, `/api/ready`, `/api/ping`" | Matches code. Additionally `GET /api/settlement` and `GET /api/agent/state` are answered inline on the listener thread *after* the gate check, so they are gated (settlement) / exempt (agent/state) as documented. | `timberbot/src/TimberbotHttpServer.cs:279-303` | HIGH | Verified, no contradiction. Logged so the next reader does not re-check. |
| C20 | `docs/events.md:38,92` "68 event types" | Code count is 68 (comm of doc table vs `PushEvent` literals: zero mismatches). | see C4 | HIGH | Verified, no contradiction. |
| C21 | `TimberbotReadV2.cs:50-56` header rule: "Background thread reads ONLY from published snapshots or explicit thread-safe services"; `docs/architecture.md:190-193` repeats it | `CollectSummary` runs on the listener thread and reads `_speedManager.CurrentSpeed`, `_dayNightCycle.DayNumber/DayProgress/PartialDayNumber`, `_gameCycleService.Cycle/CycleDay`, `_weatherService.IsHazardousWeather/TemperateWeatherDuration/HazardousWeatherDuration/CycleLengthInDays` and `_scienceService.SciencePoints` directly from live services, not from the value stores that exist for exactly these fields (`_timeStore`, `_weatherStore`, `_speedStore`, `_scienceStore`). | `timberbot/src/TimberbotReadV2.cs:717,725-726,771,786-793` vs `:491-535` | HIGH | Scalar reads, so corruption risk is low, but it is the most-called endpoint and it breaks the mod's own contract. Refines H1/H2. Phase 1 fix candidate: read from the existing snapshots. |
| C22 | `python/src/timberbot/cli/commands/watch.py:288-297` (pre-fix) and `python/tests/test_watch.py` expect `pendingRequest.goal`; `agent_prompts/connector-mode.md:12-16` says the connector "launched you with that text" | The mod publishes `pendingRequest` as `{id:int, prompt:string}` and never emits `goal` inside it. Request-mode prompts were therefore never dispatched by the WS connector. | `timberbot/src/TimberbotAgentState.cs:333-339`, `timberbot/src/TimberbotHttpServer.cs:692`, `docs/api-reference.md:223`, `docs/websocket-protocol.md:75-78` | HIGH | **Fixed 2026-09-03**: connector reads `prompt` (`goal` kept as legacy alias); tests mirror the mod wire shape. |
| C23 | `timberbot/src/TimberbotPanel.cs:71,332,574` (pre-fix): widget boots in and falls back to `autonomous` | `TimberbotAgentState.DefaultMode` is `request`; `TimberbotPure.NormalizeMode("")` also maps to `autonomous`. The panel mirrored the server mode into the dropdown without updating `_currentMode`, so the dropdown callback (`mode == _currentMode → return`) swallowed the flip to Autonomous, Launch skipped `/api/agent/request` in request mode, and prompts were saved as `goal`. | `timberbot/src/TimberbotAgentState.cs:40`, `timberbot/src/TimberbotPure.cs:234-238`, `timberbot/src/TimberbotPanel.cs:576-586,289-298,889-897` | HIGH (read; not compiled here) | **Fixed 2026-09-03**: boot mode = `TimberbotAgentState.DefaultMode`, mirror also sets `_currentMode`, fallback via new `TimberbotPure.ResolveServerMode`. Needs an in-game check on Windows (Phase 1). |
| C24 | `docs/getting-started.md:100`, `docs/timberbot.md:23`, `connector-mode.md:22-27`: in autonomous mode "the connector keeps dispatching agent runs at its configured cadence" | Pre-fix `WatchLoop` evaluated the cadence only inside `_on_state`, i.e. only when a `state` frame arrived; the mod pushes `state` only from `RaiseChanged()` (mutations and `agentStatus` flips). A cycle shorter than the interval left no pending change, so no frame, so no further cycles. | `watch.py:335-351` (pre-fix), `timberbot/src/TimberbotAgentState.cs:200-231`, `timberbot/src/TimberbotWebSocketServer.cs:129-134` | HIGH | **Fixed 2026-09-03**: dispatcher task with `next_autonomous_delay()`; docs now match behaviour. |

## Research reports vs code (added 2026-09-02)

Two research reports were supplied after the audit: report 1 ("Architektur und Implementierung eines autonomen LLM-Agenten … via MCP 2026-07-28") and report 2 ("Technische Modding-Architektur … Timberborn 1.0 und 1.1"). Same rule: code and primary sources win.

| # | Claim (report) | Code / primary source says | Anchor(s) | Confidence | Consequence |
|---|---|---|---|---|---|
| R1 | Report 1, A6 + prior-art table: Timberbot "uses BepInEx / Rust DLL injection ('Horsey Mod Framework') / renders in-game UI — IGNORE" | This fork is a native Bindito mod (`Configurator` subclass, `[Context("Game")]`); BepInEx appears only as the build-time `AssemblyPublicizer` (`PrivateAssets="all"`); no Harmony, no injection framework; the in-game panel is a Launch/Stop control widget that talks to the mod's own HTTP API, not UI automation. | `timberbot/src/TimberbotConfigurator.cs:10-29`, `timberbot/src/Timberbot.csproj:28`, `timberbot/src/manifest.json`, `AGENTS.md:37`, `timberbot/src/TimberbotPanel.cs` | HIGH | The fork remains the base. Report 1's "develop core components from scratch" is rejected. |
| R2 | Report 1, A4/A3: embed `ModelContextProtocol.AspNetCore` in the Unity process | `ModelContextProtocol.AspNetCore` has no netstandard target; only `ModelContextProtocol.Core` 2.2.0 (2026-08-13) targets netstandard2.0, with a 9-package dependency closure (`System.Text.Json` 10.x, `Microsoft.Extensions.AI.Abstractions`, `System.Net.ServerSentEvents`, …). The mod targets netstandard2.1 for Unity Mono. Report 1's own risk register names this incompatibility and proposes a hand-rolled listener as fallback — which is what the fork already has. | nuget.org `ModelContextProtocol.Core` page (fetched 2026-09-02); `timberbot/src/Timberbot.csproj:4` | HIGH (targets), UNVERIFIED (runtime loadability → Phase 1 spike S1) | ADR-001 becomes: hand-rolled endpoint on the existing listener (default) vs `Core` with custom transport vs sidecar. |
| R3 | Report 1, A5: "world mutation must be marshalled to the main thread **at tick boundaries**" (BeaverBuddies precedent) | Mutations run on the main thread under a per-frame budget in `IUpdatableSingleton.UpdateSingleton()`; nothing is tick-aligned. `Timberborn.TickSystem` is referenced by the csproj but never used. BeaverBuddies' mechanism could not be verified from its README (fetched 2026-09-02). | `timberbot/src/TimberbotService.cs:357-365`, `timberbot/src/Timberbot.csproj:73`, grep `ITickableSingleton|TickSystem` over `timberbot/src/*.cs` → 0 hits | HIGH (code), UNVERIFIED (tick hook) | ADR-005 + Phase 1 spike S2. |
| R4 | Report 1, Phase 1 "Proof of Infrastructure": first deliverable is an HTTP listener on port 3001 returning static JSON | Exists: `HttpListener` on 8085 with auth, ready gate, CORS, body cap, request logging, since well before this audit. | `timberbot/src/TimberbotHttpServer.cs:83-129,226-341` | HIGH | Phase skipped in roadmap v2. |
| R5 | Report 1, Phase 3 "Thread-Safe Action Queue": to be implemented | Exists: `ConcurrentQueue` → `DrainRequests` → `_writeQueue` → single active `ITimberbotWriteJob` with budget and settle frames. | `timberbot/src/TimberbotHttpServer.cs:140-224`, `timberbot/src/ITimberbotWriteJob.cs` | HIGH | Phase skipped; only the tick barrier (`wait_ticks`) is new. |
| R6 | Report 1, inventory: `BlockObjectPlacerService` "LOW / UNVERIFIED, from secondary sources" | Used in code as `GetMatchingPlacer(blockObjectSpec).Place(spec, placement, callback)`; compiles against the publicized `Timberborn.BlockObjectTools` assembly. Existence in the 1.0 DLLs is implied by the fork building, but not re-verified here (no DLLs in the audit container). | `timberbot/src/TimberbotPlacement.cs:2245-2249`, `timberbot/src/Timberbot.csproj` (`Timberborn.BlockObjectTools`) | HIGH (usage), MEDIUM (DLL) | Drift detector row. |
| R7 | Report 2: mod entry point is `IModStarter.StartMod(IModEnvironment)` and DI via `IConfigurator.Configure(IContainerDefinition)` | The fork has no `IModStarter` and no `Timberborn.ModSupport` reference; it uses the `Configurator` base class with `override void Configure()` and `Bind<T>()`. Both patterns may coexist in Bindito 1.0/1.1; the fork's compiles. | `timberbot/src/TimberbotConfigurator.cs:11,20-29`; grep `IModStarter|IModEnvironment|IConfigurator|IContainerDefinition|ModSupport` → 0 hits | HIGH (code), UNVERIFIED (report's API) | Not a contradiction, a gap: mod-folder resolution should come from the mod environment (Workshop installs), see roadmap slice 3a. |
| R8 | Report 2: mods load from `Documents/Timberborn/Mods` **or the Steam Workshop** | The fork hard-codes `Documents/Timberborn/Mods/Timberbot` for `settings.json`, `state.json`, `timberbot.log`, `autoload.json`, regardless of where the DLL was loaded from. | `timberbot/src/TimberbotPaths.cs:12-25`, `timberbot/src/TimberbotAutoLoad.cs:30,48` | HIGH | Workshop-installed copies would read/write the wrong folder. Roadmap 3a. |
| R9 | Report 2: `.timber` maps are ZIP archives with `version.txt` + `world.json`; `Singletons.TerrainMap.Heights.Array` is a space-separated string; row-major `Y*W+X`; X/Y horizontal, Z vertical | Z-up and X/Y-horizontal match the mod's coordinate handling. The file format is not touched by this repo and could not be verified offline. | `timberbot/src/TimberbotReadV2.cs:1107-1222` | MEDIUM (axes), UNVERIFIED (format) | Verify by unzipping a real save before trusting the report's map generator (roadmap 1.x / 3f). |
| R10 | Report 1, Key Findings: vanilla HTTP API on port 8080 offers only boolean levers/adapters (`/api/levers`, `/api/switch-on/<name>`) | Consistent with the kickoff §1; not exercised by this repo. The fork does not use the vanilla API at all. | grep `8080|switch-on|/api/levers` over `timberbot/src` → 0 hits | MEDIUM | None. |
